function setPortfolioProjectLabelAside(labelId) {
    document.getElementById(labelId).classList.add("portfolio-project-label-aside");
}

function unsetPortfolioProjectLabelAside(labelId) {
    document.getElementById(labelId).classList.remove("portfolio-project-label-aside");
}